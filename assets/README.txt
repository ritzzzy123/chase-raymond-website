Put these files in this folder:

1. kw-logo-white.png, the KW logo file you sent in chat (download it from the card I shared, then drag it here)
2. A real headshot photo (any filename, then update the HTML to point to it)
3. og-image.jpg, 1200x630px image used when the site link is shared/texted

Nothing else in the site needs to change once these are here, the code already points at this folder.
